import React, { Component } from 'react';

const Title = ({title}) => {
    return (
        <div className="page-header">
            <h1>{title}</h1>
        </div>
    );
}


export default Title;
